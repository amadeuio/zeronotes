{
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test --filter=@zeronotes/backend",
    "test:all": "turbo test",
    "test:f": "turbo test --filter=@zeronotes/frontend",
    "check": "turbo check",
    "format": "turbo format"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
